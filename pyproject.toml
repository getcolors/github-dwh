[project]
name = "package-github-dwh-blue"
version = "0.1.0"
description = "Blue Package Skill for a GitHub organization warehouse on ClickHouse"
requires-python = ">=3.11"
dependencies = [
  "blue",
  "colors-compute-blue",
  "dlt[clickhouse]>=1.20,<2",
  "dbt-clickhouse>=1.9,<2",
  "requests>=2.32,<3",
]

[tool.uv.sources]
colors-compute-blue = { git = "https://github.com/getcolors/colors-compute.git", rev = "3451a05e719b0ad6809f3c88b241a8c010b8f58b", subdirectory = "blue" }
blue = { git = "https://github.com/getcolors/blue.git", rev = "290f313ead5ca162875c33a049c880da017eae09" }

[dependency-groups]
dev = ["pytest>=8", "pytest-asyncio>=0.24"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build.targets.wheel]
packages = ["src/package_github_dwh_blue"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
